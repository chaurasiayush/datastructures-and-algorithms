package patterns.observerp.designed.observer;

public interface Observer {
    // public void update(float temp, float humid, float pressure);
    // will use pull mechanism
    public void update();
}
